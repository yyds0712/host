---
// Twikoo comment system component
// https://twikoo.js.org/
// Backend needs to be deployed to Cloudflare Workers
---

<section class="twikoo-wrapper">
	<h2 class="comments-title">评论</h2>
	<div id="tcomment"></div>
	<script is:inline>
		// Twikoo will be initialized when the page loads
		// envId should be set to your Cloudflare Worker URL after deployment
		const TWIKOO_ENV_ID = localStorage.getItem('twikoo_env_id') || 'https://twikoo.your-domain.workers.dev';
	</script>
	<script
		src="https://cdn.jsdelivr.net/npm/twikoo@1.6.41/dist/twikoo.all.min.js"
		onload="twikoo.init({ envId: TWIKOO_ENV_ID, el: '#tcomment' })"
		defer
	></script>
</section>

<style>
	.twikoo-wrapper {
		margin-top: 3em;
		padding-top: 2em;
		border-top: 1px solid var(--color-border);
	}
	.comments-title {
		margin-bottom: 1em;
	}
</style>
